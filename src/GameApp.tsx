import { useEffect, useMemo, useState } from "react";
import ClashCardsV2 from "./ClashCardsV2";
import AuthGate from "./components/AuthGate";
import LobbyHome from "./components/game/LobbyHome";
import { adminCoins, isAdminEmail } from "./lib/admin";
import { avatarItems } from "./lib/cosmetics";
import { claimDailyReward, getDailyRewardState } from "./lib/dailyRewards";
import { gameIdentity } from "./lib/gameIdentity";
import { useAuthUser } from "./lib/useAuthUser";

type Screen = "home" | "play" | "profile" | "shop" | "modes";
const storage = gameIdentity.storage;
function readNumber(key: string) { return Number(localStorage.getItem(`${storage}:${key}`) || 0); }
function writeNumber(key: string, value: number) { localStorage.setItem(`${storage}:${key}`, String(value)); }
function readText(key: string, fallback = "") { return localStorage.getItem(`${storage}:${key}`) || fallback; }
function writeText(key: string, value: string) { localStorage.setItem(`${storage}:${key}`, value); }
function ownedAvatars() { return new Set((localStorage.getItem(`${storage}:ownedAvatars`) || "default").split(",").filter(Boolean)); }
function saveOwned(set: Set<string>) { localStorage.setItem(`${storage}:ownedAvatars`, Array.from(set).join(",")); }
function tone() { try { const Ctx = window.AudioContext || (window as unknown as { webkitAudioContext: typeof AudioContext }).webkitAudioContext; const ctx = new Ctx(); const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.frequency.value = 520; gain.gain.setValueAtTime(.001, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(.065, ctx.currentTime + .015); gain.gain.exponentialRampToValueAtTime(.001, ctx.currentTime + .11); osc.connect(gain); gain.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + .13); } catch {} }
function BottomNav({ screen, setScreen }: { screen: Screen; setScreen: (screen: Screen) => void }) { const items: { id: Screen; label: string; icon: string }[] = [{ id: "home", label: "Lobby", icon: "🏠" }, { id: "modes", label: "Jogos", icon: "🎮" }, { id: "shop", label: "Loja", icon: "🛒" }, { id: "profile", label: "Perfil", icon: "👤" }]; return <nav className="fixed bottom-3 left-1/2 z-50 grid w-[min(560px,94vw)] -translate-x-1/2 grid-cols-4 gap-2 rounded-[1.35rem] border border-white/10 bg-black/80 p-2 shadow-[0_18px_55px_rgba(0,0,0,.72)] backdrop-blur-xl md:bottom-5">{items.map((item) => <button key={item.id} onClick={() => { tone(); setScreen(item.id); }} className={`rounded-[1rem] px-2 py-2 text-center text-[11px] font-black transition active:scale-[.96] ${screen === item.id ? "bg-yellow-300 text-black shadow-[0_0_24px_rgba(250,204,21,.35)]" : "bg-white/[0.045] text-white/65"}`}><span className="block text-base">{item.icon}</span>{item.label}</button>)}</nav>; }
function Stat({ label, value }: { label: string; value: string | number }) { return <div className="rounded-[1.55rem] border border-white/10 bg-white/[0.045] p-4"><p className="text-[10px] uppercase tracking-[0.22em] text-zinc-500">{label}</p><strong className="mt-2 block text-2xl text-white">{value}</strong></div>; }

export default function GameApp() {
  const { user, setUser, loading } = useAuthUser(); const isAdmin = isAdminEmail(user?.email); const [screen, setScreen] = useState<Screen>("home"); const [showSplash, setShowSplash] = useState(() => !sessionStorage.getItem("thk-splash-seen")); const [shopMessage, setShopMessage] = useState("Cosméticos apenas. Nada de pay-to-win."); const [refresh, setRefresh] = useState(0); const [playerName, setPlayerName] = useState(readText("playerName", "Kairós")); const [selectedMode, setSelectedMode] = useState(gameIdentity.modes[0]); const [daily, setDaily] = useState(getDailyRewardState()); const [lobbyMessage, setLobbyMessage] = useState("Lobby pronto. Clash Cards está disponível.");
  useEffect(() => { document.documentElement.style.background = "#000"; document.body.style.background = "#000"; document.body.style.overflowX = "hidden"; document.body.style.margin = "0"; if (showSplash) { const timer = window.setTimeout(() => { sessionStorage.setItem("thk-splash-seen", "1"); setShowSplash(false); }, 650); return () => window.clearTimeout(timer); } }, [showSplash]);
  const roomCode = readText("roomCode", "THK-742"); const stats = useMemo(() => { const rawCoins = readNumber("coins"); const coins = adminCoins(rawCoins, user?.email); const xp = readNumber("xp"); const wins = readNumber("wins"); const losses = readNumber("losses"); const level = Math.floor(xp / 100) + 1; const total = Math.max(1, wins + losses); const winRate = Math.round((wins / total) * 100); const currentLevelXp = xp % 100; return { coins, xp, wins, losses, level, winRate, currentLevelXp }; }, [screen, refresh, user?.email]); const equippedId = readText("equippedAvatar", "default"); const owned = ownedAvatars(); const equipped = avatarItems.find((item) => item.id === equippedId) || avatarItems[0];
  function forceRefresh() { setRefresh((value) => value + 1); } function saveName() { const clean = playerName.trim().slice(0, 18) || "Jogador THK"; setPlayerName(clean); writeText("playerName", clean); setLobbyMessage("Nome atualizado."); forceRefresh(); } function copyRoomCode() { navigator.clipboard?.writeText(roomCode); setLobbyMessage("Código da sala copiado."); } function claimDaily() { const result = claimDailyReward(); setDaily(getDailyRewardState()); setLobbyMessage(result.claimed ? `Recompensa diária coletada: +${result.reward} moedas.` : "Você já coletou a recompensa de hoje."); forceRefresh(); } function buyOrEquip(id: string, price: number) { const currentOwned = ownedAvatars(); if (currentOwned.has(id)) { writeText("equippedAvatar", id); setShopMessage("Avatar equipado."); forceRefresh(); return; } if (!isAdmin) { const coins = readNumber("coins"); if (coins < price) { setShopMessage(`Moedas insuficientes. Faltam ${price - coins} moedas.`); return; } writeNumber("coins", coins - price); } currentOwned.add(id); saveOwned(currentOwned); writeText("equippedAvatar", id); setShopMessage(isAdmin ? "ADM: avatar liberado e equipado." : "Avatar comprado e equipado."); forceRefresh(); }
  if (screen === "play") return <ClashCardsV2 onBack={() => setScreen("home")} />;
  if (showSplash) return <main className="grid min-h-screen place-items-center overflow-hidden bg-black text-white"><div className="relative text-center"><div className="absolute left-1/2 top-1/2 h-60 w-60 -translate-x-1/2 -translate-y-1/2 rounded-full bg-violet-600/25 blur-3xl" /><div className="relative mx-auto grid h-24 w-24 place-items-center rounded-[2rem] border border-white/10 bg-gradient-to-br from-violet-500 via-fuchsia-600 to-black text-5xl shadow-[0_0_80px_rgba(139,92,246,.35)]">⚡</div><p className="mt-6 text-[11px] font-black uppercase tracking-[0.55em] text-yellow-300">THKLAYUS</p><h1 className="mt-2 text-4xl font-black">Clash Room</h1></div></main>;
  return <main className="min-h-screen overflow-x-hidden bg-[radial-gradient(circle_at_50%_0%,rgba(124,58,237,.34),transparent_24rem),linear-gradient(180deg,#050008,#000)] text-white"><div className="mx-auto min-h-screen w-full max-w-[1200px] px-2 pb-24 pt-2 md:px-4 md:pt-4"><header className="mx-auto mb-2 flex w-full max-w-[1040px] items-center justify-between gap-3 rounded-[1.35rem] border border-white/10 bg-black/35 px-3 py-3 backdrop-blur-xl"><div className="min-w-0"><p className="text-[9px] uppercase tracking-[0.32em] text-yellow-300">{gameIdentity.brand}</p><h1 className="truncate text-2xl font-black tracking-tight md:text-3xl">{gameIdentity.appName}</h1><p className="text-[9px] uppercase tracking-[0.2em] text-zinc-500">{loading ? "Carregando conta..." : isAdmin ? "ADM Alpha Arena" : gameIdentity.version}</p></div><div className="flex shrink-0 items-center gap-2"><div className="rounded-full border border-yellow-300/25 bg-yellow-300/10 px-3 py-2 text-xs font-black shadow-[0_0_30px_rgba(250,204,21,.22)]">🪙 {stats.coins}</div><div className="rounded-full border border-violet-300/20 bg-violet-400/10 px-3 py-2 text-xs font-black">Nv. {stats.level}</div></div></header><div className="mx-auto w-full max-w-[1040px] transition-all duration-300 ease-out">{screen === "home" && <LobbyHome playerName={readText("playerName", user?.email?.split("@")[0] || "Kairós")} equipped={equipped} stats={stats} daily={daily} roomCode={roomCode} lobbyMessage={isAdmin ? "ADM ativo: economia liberada para testes." : lobbyMessage} selectedMode={selectedMode} onPlay={() => setScreen("play")} onClaimDaily={claimDaily} onCopyRoomCode={copyRoomCode} onSelectMode={setSelectedMode} onLobbyMessage={setLobbyMessage} />}{screen === "modes" && <section className="grid gap-4 md:grid-cols-2">{gameIdentity.modes.map((mode, index) => <button key={mode.id} onClick={() => index === 0 ? setScreen("play") : setSelectedMode(mode)} className={`rounded-[2rem] border p-6 text-left transition active:scale-[.99] ${index === 0 ? "border-violet-300/30 bg-violet-500/10 shadow-[0_0_55px_rgba(139,92,246,.14)]" : "border-white/10 bg-white/[0.045] opacity-85"}`}><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-black text-zinc-200">{mode.status}</span><h2 className="mt-4 text-3xl font-black">{mode.title}</h2><p className="mt-2 text-sm text-zinc-400">{mode.description}</p></button>)}</section>}{screen === "profile" && <section className="grid gap-5 md:grid-cols-[1fr_1fr]"><div className="space-y-5"><div className="rounded-[2rem] border border-white/10 bg-white/[0.055] p-6 backdrop-blur-xl"><div className="flex items-center gap-4"><div className={`grid h-24 w-24 place-items-center rounded-[1.7rem] bg-gradient-to-br ${equipped?.gradient || "from-violet-700 to-cyan-400"} text-5xl shadow-2xl`}>{equipped?.emoji || "⚡"}</div><div><p className="text-xs uppercase tracking-[0.28em] text-violet-300">Perfil</p><h2 className="text-3xl font-black">{readText("playerName", user?.email?.split("@")[0] || "Kairós")}</h2><p className="text-zinc-400">Avatar: {equipped?.name || "Padrão"}</p>{isAdmin && <p className="mt-2 inline-flex rounded-full bg-amber-300 px-3 py-1 text-xs font-black text-black">ADM · teste ilimitado</p>}</div></div><div className="mt-5 flex gap-2"><input value={playerName} onChange={(event) => setPlayerName(event.target.value)} className="min-w-0 flex-1 rounded-2xl border border-white/10 bg-black/40 px-4 py-3 font-bold outline-none focus:border-violet-300" placeholder="Nome do jogador" /><button onClick={saveName} className="rounded-2xl bg-violet-300 px-5 py-3 font-black text-black">Salvar</button></div></div><AuthGate user={user} onUserChange={setUser} /></div><div className="grid gap-4 sm:grid-cols-2"><Stat label="Moedas" value={stats.coins} /><Stat label="XP total" value={stats.xp} /><Stat label="Vitórias" value={stats.wins} /><Stat label="Derrotas" value={stats.losses} /><Stat label="Win rate" value={`${stats.winRate}%`} /><Stat label="Nível" value={stats.level} /></div></section>}{screen === "shop" && <section className="rounded-[2rem] border border-white/10 bg-white/[0.045] p-5 backdrop-blur-xl"><div className="mb-5 flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[0.28em] text-yellow-300">Loja</p><h2 className="text-3xl font-black">Avatares Neon</h2><p className="mt-1 text-sm text-zinc-400">{isAdmin ? "ADM ativo: todos os cosméticos podem ser liberados para teste." : shopMessage}</p></div><div className="rounded-full border border-yellow-300/25 bg-yellow-300/10 px-4 py-2 text-sm font-black">🪙 {stats.coins}</div></div><div className="grid gap-4 sm:grid-cols-2 md:grid-cols-3">{avatarItems.map((item) => { const isOwned = owned.has(item.id); const isEquipped = equipped?.id === item.id; return <button key={item.id} onClick={() => buyOrEquip(item.id, item.price)} className={`rounded-[2rem] border p-4 text-left transition active:scale-[.99] ${isEquipped ? "border-yellow-300 bg-yellow-300/15" : "border-white/10 bg-black/35 hover:bg-white/[0.06]"}`}><div className={`grid h-24 place-items-center rounded-[1.5rem] bg-gradient-to-br ${item.gradient} text-5xl shadow-[0_20px_55px_rgba(0,0,0,.42)]`}>{item.emoji}</div><div className="mt-4 flex items-start justify-between gap-2"><div><strong className="block text-lg">{item.name}</strong><p className="text-xs uppercase tracking-[0.2em] text-zinc-500">{item.rarity}</p></div><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-black">{isEquipped ? "Usando" : isOwned ? "Equipar" : isAdmin ? "ADM" : `🪙 ${item.price}`}</span></div></button>; })}</div></section>}</div></div><BottomNav screen={screen} setScreen={setScreen} /></main>;
}
