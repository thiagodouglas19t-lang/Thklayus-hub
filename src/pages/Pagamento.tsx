import { useEffect, useState } from "react";
import { supabase } from "../lib/supabase";
import { professionalCourses } from "../data/courses";

type Produto = { id: string; nome: string; descricao: string; preco: string; tipo: "purchase" | "order" };
const cursoPrincipal = professionalCourses.find((course) => course.id === "tecnico-eletronica");
const produtos: Produto[] = [
  { id: "tecnico-eletronica", nome: "Técnico em Eletrônica • Fundamentos Completos", descricao: "Curso principal: eletrônica do zero, segurança, componentes, multímetro, esquemas e projetos práticos.", preco: "R$ 29,90", tipo: "purchase" },
  { id: "powerpoint-pro", nome: "PowerPoint Profissional", descricao: "Aprenda a criar apresentações bonitas para escola, trabalho e clientes.", preco: "R$ 9,90", tipo: "purchase" },
  { id: "informatica-zero-iniciante", nome: "Informática do Zero", descricao: "Curso de entrada para aprender organização digital, internet, arquivos e ferramentas básicas.", preco: "R$ 4,90", tipo: "purchase" },
  { id: "servico-escolar", nome: "Serviço Escolar Personalizado", descricao: "Pedido de apresentação, resumo, mapa mental, trabalho ou material de estudo.", preco: "A combinar", tipo: "order" },
];
const chavePix = "64448272-b9c0-41ac-8755-2704ae0cca92";

export default function Pagamento() {
  const [produto, setProduto] = useState<Produto>(produtos[0]);
  const [loading, setLoading] = useState(false);
  const [compraId, setCompraId] = useState("");
  const [userId, setUserId] = useState("");
  const [erro, setErro] = useState("");
  const [sucesso, setSucesso] = useState("");
  const [obs, setObs] = useState("");
  useEffect(() => { carregarUsuario(); }, []);
  async function carregarUsuario() { const { data } = await supabase.auth.getUser(); setUserId(data.user?.id ?? ""); }
  async function copiar(texto: string, mensagemOk: string) { try { await navigator.clipboard.writeText(texto); setSucesso(mensagemOk); } catch { setErro("Não consegui copiar nesse navegador. Copie manualmente: " + texto); } }
  async function copiarPix() { await copiar(chavePix, "Chave Pix copiada. Pague o valor exato e depois registre a compra aqui no app."); }
  async function copiarValor() { await copiar(produto.preco, "Valor copiado. Pague exatamente esse valor no Pix."); }
  async function copiarUserId() { if (!userId) { setErro("Entre na sua conta para gerar seu ID de usuário."); return; } await copiar(userId, "ID do usuário copiado."); }
  async function copiarProdutoId() { await copiar(produto.id, "ID do curso/produto copiado."); }

  async function registrarCompra() {
    setErro(""); setSucesso(""); setLoading(true);
    const { data: userData } = await supabase.auth.getUser();
    if (!userData.user) { setLoading(false); setErro("Entre na sua conta antes de registrar uma compra."); return; }
    setUserId(userData.user.id);
    const { data: thread, error: threadError } = await supabase.from("chat_threads").insert({ user_id: userData.user.id, type: produto.tipo, title: produto.tipo === "purchase" ? `Compra - ${produto.nome}` : `Pedido - ${produto.nome}`, status: produto.tipo === "purchase" ? "em análise" : "pendente", course_id: produto.id, course_title: produto.nome, price: produto.preco, total_price: produto.preco }).select("id").single();
    if (threadError) { setLoading(false); setErro("Erro ao registrar compra: " + threadError.message); return; }
    const texto = produto.tipo === "purchase" ? `🛒 Compra registrada dentro do app\n\nCurso: ${produto.nome}\nID do curso: ${produto.id}\nValor exato pago: ${produto.preco}\nChave Pix usada: ${chavePix}\nID do usuário: ${userData.user.id}\nID da compra: ${thread.id}\nStatus: aguardando conferência do ADM.\n\nObservação do cliente: ${obs.trim() || "sem observação"}\n\nDepois que o ADM confirmar o Pix no banco, ele clica em Liberar curso e o acesso aparece em Meus Cursos.` : `📦 Pedido registrado dentro do app\n\nProduto: ${produto.nome}\nID do produto: ${produto.id}\nValor: ${produto.preco}\nID do usuário: ${userData.user.id}\nID do pedido: ${thread.id}\nObservação do cliente: ${obs.trim() || "sem observação"}`;
    const { error: messageError } = await supabase.from("chat_messages").insert({ thread_id: thread.id, user_id: userData.user.id, content: texto });
    if (messageError) { setLoading(false); setErro("A compra foi criada, mas deu erro ao salvar a mensagem: " + messageError.message); setCompraId(thread.id); return; }
    setCompraId(thread.id); setLoading(false); setObs(""); setSucesso("Compra registrada no chat do app. Agora aguarde o ADM confirmar o Pix e liberar seu curso.");
  }

  const modulos = cursoPrincipal?.modules ?? [];
  const aulas = modulos.reduce((sum, modulo) => sum + modulo.lessons.length, 0);
  return <div className="mx-auto max-w-6xl space-y-6"><section className="relative overflow-hidden rounded-[2.6rem] border border-white/10 bg-white/[0.035] p-6 shadow-2xl shadow-black/40 backdrop-blur-xl md:p-10"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_15%_0%,rgba(251,191,36,0.22),transparent_32%),radial-gradient(circle_at_90%_10%,rgba(59,130,246,0.22),transparent_30%)]" /><div className="relative grid gap-8 lg:grid-cols-[1.05fr_0.95fr] lg:items-center"><div><span className="rounded-full border border-amber-300/25 bg-amber-500/10 px-4 py-2 text-xs font-black uppercase tracking-[0.18em] text-amber-100">Oferta principal • AprendaJá</span><h1 className="mt-6 text-4xl font-black leading-[1.02] tracking-[-0.06em] md:text-7xl">Aprenda eletrônica do zero ao primeiro circuito funcional.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-zinc-300 md:text-lg">Mesmo começando do básico, você aprende segurança, Lei de Ohm, resistores, LEDs, capacitores, protoboard, multímetro, esquemas e projetos práticos.</p><div className="mt-6 flex flex-wrap gap-2">{[`${modulos.length} módulos`, `${aulas} aulas`, "Certificado", "Projetos práticos", "Chat dentro do app"].map((item) => <span key={item} className="rounded-full border border-white/10 bg-black/35 px-4 py-2 text-sm font-black text-zinc-200">{item}</span>)}</div><div className="mt-7"><p className="text-sm font-black uppercase tracking-[0.18em] text-zinc-500">Valor exato para pagar</p><p className="text-5xl font-black text-emerald-200">R$ 29,90</p><p className="mt-1 text-xs font-black uppercase tracking-[0.16em] text-amber-200">Preço de lançamento honesto</p></div></div><div className="rounded-[2rem] border border-white/10 bg-black/50 p-5"><p className="text-6xl">⚡</p><h2 className="mt-4 text-3xl font-black">O que você vai aprender</h2><div className="mt-5 space-y-3">{["Entender energia elétrica sem confusão", "Calcular resistores com Lei de Ohm", "Montar circuitos simples em baixa tensão", "Usar multímetro no básico", "Ler esquemas simples", "Criar projeto final documentado"].map((item) => <div key={item} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4 text-sm font-bold text-zinc-200">✓ {item}</div>)}</div></div></div></section>
  <section className="grid gap-4 md:grid-cols-4">{[["01", "Escolha o curso", "Selecione o produto que quer comprar."], ["02", "Pague no Pix", "Use a chave Pix e o valor exato da tela."], ["03", "Registre no app", "Clique em Registrar compra e a conversa nasce no chat."], ["04", "ADM libera", "Depois de conferir, o ADM clica em Liberar curso."]].map(([num, title, text]) => <div key={String(num)} className="rounded-3xl border border-white/10 bg-zinc-950 p-5"><p className="text-3xl font-black text-blue-200">{num}</p><h3 className="mt-3 font-black text-white">{title}</h3><p className="mt-2 text-sm leading-6 text-zinc-400">{text}</p></div>)}</section>
  <section className="rounded-[2rem] border border-white/10 bg-white/[0.035] p-5 md:p-6"><h2 className="text-3xl font-black">Módulos do curso principal</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{modulos.map((modulo, index) => <div key={modulo.title} className="rounded-2xl border border-white/10 bg-black/35 p-4"><p className="text-xs font-black uppercase tracking-[0.16em] text-zinc-500">Módulo {index + 1}</p><h3 className="mt-1 font-black text-white">{modulo.title}</h3><p className="mt-2 text-sm text-zinc-500">{modulo.lessons.length} aulas práticas</p></div>)}</div></section>
  <section className="grid gap-4 lg:grid-cols-[1fr_0.8fr]"><div className="rounded-[2rem] border border-white/10 bg-black/50 p-6 backdrop-blur-xl"><h2 className="text-2xl font-black text-white">Comprar no Pix</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{produtos.map((item) => { const ativo = item.id === produto.id; return <button key={item.id} onClick={() => { setProduto(item); setCompraId(""); setErro(""); setSucesso(""); }} className={`rounded-3xl border p-5 text-left transition active:scale-95 ${ativo ? "border-blue-400 bg-blue-500/15" : "border-white/10 bg-white/[0.04]"}`}><h3 className="font-black text-white">{item.nome}</h3><p className="mt-2 text-sm text-zinc-400">{item.descricao}</p><p className="mt-3 break-all text-xs text-zinc-500">ID: {item.id}</p><p className="mt-4 text-2xl font-black text-blue-300">{item.preco}</p></button>; })}</div><div className="mt-5 rounded-[2rem] border border-emerald-400/20 bg-emerald-500/10 p-5"><p className="text-xs font-black uppercase tracking-[0.2em] text-emerald-200">Pague exatamente</p><p className="mt-2 text-5xl font-black text-white">{produto.preco}</p><p className="mt-2 text-sm font-bold text-emerald-100/80">Produto: {produto.nome}</p><button onClick={copiarValor} className="mt-4 rounded-2xl bg-white px-5 py-3 text-sm font-black text-black">Copiar valor</button></div><div className="mt-4 rounded-[2rem] border border-blue-400/20 bg-blue-500/10 p-5"><p className="text-xs font-black uppercase tracking-[0.2em] text-blue-200">Chave Pix aleatória</p><p className="mt-3 break-all rounded-2xl bg-black/45 p-4 text-xl font-black text-white md:text-2xl">{chavePix}</p><button onClick={copiarPix} className="mt-4 w-full rounded-2xl bg-white px-5 py-4 font-black text-black">Copiar chave Pix</button></div><div className="mt-4 rounded-2xl border border-violet-400/20 bg-violet-500/10 p-4 text-sm text-violet-100"><p>Curso/produto escolhido: <span className="font-black text-white">{produto.nome}</span></p><p className="mt-1 break-all">ID do curso/produto: <span className="font-black text-white">{produto.id}</span></p><button onClick={copiarProdutoId} className="mt-3 rounded-xl bg-white px-4 py-2 text-xs font-black text-black">Copiar ID do curso</button></div><div className="mt-4 rounded-2xl border border-amber-400/20 bg-amber-500/10 p-4 text-sm text-amber-100"><p className="font-black">Tudo fica dentro do app</p><p className="mt-1">Depois de pagar, clique em Registrar compra. O pedido aparece no chat do app e no ADM.</p><p className="mt-3 break-all text-xs">Seu ID: <span className="font-black">{userId || "entre na conta para aparecer"}</span></p><button onClick={copiarUserId} className="mt-3 rounded-xl bg-white px-4 py-2 text-xs font-black text-black">Copiar meu ID</button></div><textarea value={obs} onChange={(e) => setObs(e.target.value)} placeholder="Observação opcional: coloque aqui nome do pagador, horário do Pix ou qualquer detalhe útil..." className="mt-4 min-h-28 w-full rounded-2xl border border-white/10 bg-black/45 p-4 text-sm font-bold text-white outline-none placeholder:text-zinc-600" />{compraId && <p className="mt-4 rounded-2xl border border-emerald-400/20 bg-emerald-500/10 p-4 text-sm text-emerald-100">ID da compra no app: <span className="font-black">{compraId}</span></p>}{erro && <p className="mt-4 rounded-2xl border border-red-400/20 bg-red-500/10 p-4 text-sm text-red-100">{erro}</p>}{sucesso && <p className="mt-4 rounded-2xl border border-emerald-400/20 bg-emerald-500/10 p-4 text-sm text-emerald-100">{sucesso}</p>}<button onClick={registrarCompra} disabled={loading} className="mt-6 w-full rounded-2xl bg-white px-5 py-5 text-center text-lg font-black text-black disabled:opacity-60">{loading ? "Registrando..." : compraId ? "Compra registrada" : "Registrar compra no app"}</button></div><div className="space-y-4"><div className="rounded-[2rem] border border-white/10 bg-white/[0.035] p-6"><h3 className="text-2xl font-black text-white">Como funciona</h3><div className="mt-5 space-y-3">{[["1", "Escolha o curso."], ["2", "Pague o valor exato no Pix."], ["3", "Registre a compra dentro do app."], ["4", "Acompanhe pelo chat/suporte do app."], ["5", "ADM libera o acesso." ]].map(([num, text]) => <div key={num} className="flex gap-3 rounded-2xl border border-white/10 bg-black/35 p-4"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-white font-black text-black">{num}</span><p className="text-sm font-bold text-zinc-300">{text}</p></div>)}</div></div><div className="rounded-[2rem] border border-emerald-400/20 bg-emerald-500/10 p-6"><h3 className="text-xl font-black text-emerald-100">Sem WhatsApp obrigatório</h3><p className="mt-2 text-sm leading-6 text-emerald-100/75">O registro da compra, IDs, valor e curso ficam salvos no app. O ADM vê no painel e libera por lá.</p></div></div></section></div>;
}
