import { useEffect, useMemo, useState } from "react";
import { supabase } from "../lib/supabase";
import { professionalCourses } from "../data/courses";
import { getQualityScore } from "../data/courseQuality";
import { formatBrasiliaDateTime } from "../lib/date";

type Thread = { id: string; user_id: string; type: "ticket" | "purchase" | "order"; title: string; status: string; course_id?: string | null; course_title?: string | null; total_price?: string | null; price?: string | null; comprovante_url?: string | null; created_at?: string };
const tabs = ["todos", "compras", "pedidos", "tickets", "pendentes", "finalizados"];
const finalizados = ["compra finalizada", "pedido finalizado", "fechado", "entregue", "compra recusada", "resolvido", "encerrado"];
const aprovados = ["compra aprovada", "compra finalizada"];
function normalize(value?: string | null) { return String(value ?? "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").trim(); }
function parseMoney(value?: string | null) { if (!value) return 0; const clean = String(value).replace(/[^0-9,.-]/g, "").replace(".", "").replace(",", "."); const parsed = Number(clean); return Number.isFinite(parsed) ? parsed : 0; }
function money(value: number) { return new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(value); }
function formatDate(value?: string) { return formatBrasiliaDateTime(value); }
function statusStyle(status: string) { const value = normalize(status); if (["compra aprovada", "compra finalizada", "pedido finalizado", "entregue"].includes(value)) return "border-emerald-400/30 bg-emerald-500/10 text-emerald-200"; if (["compra recusada", "recusado"].includes(value)) return "border-red-400/30 bg-red-500/10 text-red-200"; if (["em analise", "pendente", "aberto"].includes(value)) return "border-amber-400/30 bg-amber-500/10 text-amber-200"; return "border-zinc-500/30 bg-zinc-500/10 text-zinc-300"; }
function typeLabel(type: Thread["type"]) { if (type === "purchase") return "Compra"; if (type === "order") return "Pedido"; return "Ticket"; }
function canRelease(item: Thread) { return item.type === "purchase" && Boolean(item.course_id) && !aprovados.includes(normalize(item.status)); }
function qualityClass(score: number) { if (score >= 90) return "border-emerald-400/25 bg-emerald-500/10 text-emerald-100"; if (score >= 70) return "border-amber-400/25 bg-amber-500/10 text-amber-100"; return "border-red-400/25 bg-red-500/10 text-red-100"; }

export default function Admin() {
  const [threads, setThreads] = useState<Thread[]>([]);
  const [loading, setLoading] = useState(true);
  const [tab, setTab] = useState("compras");
  const [erro, setErro] = useState("");
  const [busca, setBusca] = useState("");
  const [updatingId, setUpdatingId] = useState<string | null>(null);
  const [manualUserId, setManualUserId] = useState("");
  const [manualCourseId, setManualCourseId] = useState("");
  const [manualCourseTitle, setManualCourseTitle] = useState("");
  const [manualLoading, setManualLoading] = useState(false);
  const [showCourseIds, setShowCourseIds] = useState(true);
  const [showQuality, setShowQuality] = useState(true);
  useEffect(() => { carregar(); }, []);

  async function carregar() { setLoading(true); setErro(""); const { data, error } = await supabase.from("chat_threads").select("*").order("created_at", { ascending: false }); if (error) { setErro(error.message); setLoading(false); return; } setThreads((data ?? []) as Thread[]); setLoading(false); }
  async function copiar(texto: string) { try { await navigator.clipboard.writeText(texto); alert("Copiado!"); } catch { alert("Não foi possível copiar nesse navegador."); } }
  function preencherCurso(id: string, title: string) { setManualCourseId(id); setManualCourseTitle(title); }

  async function chamarLiberacao(userId: string, courseId: string, courseTitle: string) {
    const session = await supabase.auth.getSession(); const token = session.data.session?.access_token;
    if (!token) throw new Error("Sessão expirada. Saia e entre novamente.");
    const res = await fetch(`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/liberar-curso-manual`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify({ user_id: userId, produto_id: courseId, course_title: courseTitle }) });
    const json = await res.json().catch(() => ({})); if (!res.ok) throw new Error(json.error || "Erro ao liberar curso.");
  }

  async function liberarCursoManual() {
    const userId = manualUserId.trim(); const courseId = manualCourseId.trim(); const courseTitle = manualCourseTitle.trim() || courseId;
    if (!userId || !courseId) { alert("Preencha o ID do usuário e o ID do curso."); return; }
    if (!confirm(`Liberar curso?\n\nUsuário: ${userId}\nCurso: ${courseTitle}\nID: ${courseId}`)) return;
    setManualLoading(true);
    try { await chamarLiberacao(userId, courseId, courseTitle); setManualUserId(""); setManualCourseId(""); setManualCourseTitle(""); alert("Curso liberado."); }
    catch (error) { alert("Erro ao liberar curso: " + (error as Error).message); }
    finally { setManualLoading(false); }
  }

  async function liberarCursoDaCompra(item: Thread) {
    if (!item.course_id) { alert("Essa compra não tem ID do curso. Use a liberação manual acima."); return; }
    if (aprovados.includes(normalize(item.status))) { alert("Essa compra já está aprovada ou finalizada."); return; }
    if (updatingId === item.id) return;
    if (!confirm(`Conferiu o Pix no banco?\n\nSe sim, vou liberar:\n${item.course_title || item.course_id}\npara o usuário ${item.user_id}`)) return;
    setUpdatingId(item.id);
    try {
      await chamarLiberacao(item.user_id, item.course_id, item.course_title || item.course_id);
      await supabase.from("chat_threads").update({ status: "compra aprovada" }).eq("id", item.id);
      await supabase.from("chat_messages").insert({ thread_id: item.id, user_id: "00000000-0000-0000-0000-000000000000", content: "✅ Curso liberado pelo ADM. Acesse em Meus Cursos." });
      setThreads((current) => current.map((thread) => thread.id === item.id ? { ...thread, status: "compra aprovada" } : thread));
      alert("Curso liberado e compra marcada como aprovada.");
    } catch (error) { alert("Erro ao liberar: " + (error as Error).message); }
    finally { setUpdatingId(null); }
  }

  async function atualizarStatus(id: string, status: string) { if (updatingId === id) return; setUpdatingId(id); const { error } = await supabase.from("chat_threads").update({ status }).eq("id", id); if (error) { alert("Erro: " + error.message); setUpdatingId(null); return; } setThreads((current) => current.map((item) => item.id === id ? { ...item, status } : item)); setUpdatingId(null); }

  const filtrados = useMemo(() => { const termo = normalize(busca); return threads.filter((item) => { if (tab === "compras" && item.type !== "purchase") return false; if (tab === "pedidos" && item.type !== "order") return false; if (tab === "tickets" && item.type !== "ticket") return false; if (tab === "pendentes" && !["em analise", "pendente", "aberto", "em producao"].includes(normalize(item.status))) return false; if (tab === "finalizados" && !finalizados.includes(normalize(item.status))) return false; if (!termo) return true; return normalize(`${item.title} ${item.status} ${item.course_title ?? ""} ${item.course_id ?? ""} ${item.id} ${item.user_id}`).includes(termo); }); }, [threads, tab, busca]);
  const receitaAprovada = threads.filter((item) => aprovados.includes(normalize(item.status))).reduce((sum, item) => sum + parseMoney(item.total_price || item.price), 0);
  const receitaPendente = threads.filter((item) => item.type === "purchase" && ["em analise", "pendente"].includes(normalize(item.status))).reduce((sum, item) => sum + parseMoney(item.total_price || item.price), 0);
  const qualityList = professionalCourses.map((course) => ({ course, quality: getQualityScore(course) })).sort((a, b) => a.quality.score - b.quality.score);
  const cursosCriticos = qualityList.filter((item) => item.quality.score < 70).length;

  return <div className="space-y-6"><section className="rounded-[2.4rem] border border-white/10 bg-white/[0.035] p-6 shadow-2xl shadow-black/30"><div className="flex flex-wrap items-end justify-between gap-4"><div><span className="rounded-full border border-emerald-400/25 bg-emerald-500/10 px-4 py-2 text-xs font-black uppercase tracking-[0.2em] text-emerald-100">ADM • seguro</span><h2 className="mt-5 text-4xl font-black md:text-6xl">Painel de controle</h2><p className="mt-2 text-zinc-400">Conferiu o Pix? Libere uma vez. Curso fraco? Melhore antes de vender.</p></div><button onClick={carregar} className="rounded-2xl bg-white px-5 py-3 font-black text-black">Atualizar</button></div></section>
  <section className="grid gap-4 md:grid-cols-4"><div className="rounded-3xl border border-emerald-400/20 bg-emerald-500/10 p-5"><p className="text-sm text-emerald-100/70">Receita confirmada</p><p className="mt-1 text-3xl font-black text-emerald-100">{money(receitaAprovada)}</p></div><div className="rounded-3xl border border-amber-400/20 bg-amber-500/10 p-5"><p className="text-sm text-amber-100/70">Aguardando Pix</p><p className="mt-1 text-3xl font-black text-amber-100">{money(receitaPendente)}</p></div><div className="rounded-3xl border border-white/10 bg-white/[0.035] p-5"><p className="text-sm text-zinc-500">Compras pendentes</p><p className="mt-1 text-3xl font-black">{threads.filter((i) => i.type === "purchase" && ["em analise", "pendente"].includes(normalize(i.status))).length}</p></div><div className={`rounded-3xl border p-5 ${cursosCriticos ? "border-red-400/20 bg-red-500/10" : "border-emerald-400/20 bg-emerald-500/10"}`}><p className={cursosCriticos ? "text-sm text-red-100/70" : "text-sm text-emerald-100/70"}>Cursos críticos</p><p className={cursosCriticos ? "mt-1 text-3xl font-black text-red-100" : "mt-1 text-3xl font-black text-emerald-100"}>{cursosCriticos}</p></div></section>
  <section className="rounded-[2rem] border border-amber-400/20 bg-amber-500/10 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="text-2xl font-black text-amber-100">Validação de qualidade dos cursos</h3><p className="mt-1 text-sm text-amber-100/75">Antes de vender forte, confira se o curso parece completo. Curso abaixo de 70% pode gerar reclamação.</p></div><button onClick={() => setShowQuality((value) => !value)} className="rounded-2xl border border-amber-300/25 px-4 py-2 text-sm font-black text-amber-100">{showQuality ? "Ocultar" : "Mostrar"}</button></div>{showQuality && <div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{qualityList.map(({ course, quality }) => <div key={course.id} className={`rounded-2xl border p-4 ${qualityClass(quality.score)}`}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.16em] opacity-70">{course.category}</p><h4 className="mt-1 text-base font-black">{course.title}</h4><p className="mt-1 text-xs opacity-75">{course.price} • {course.duration}</p></div><div className="text-right"><p className="text-3xl font-black">{quality.score}%</p><p className="text-xs font-black opacity-80">{quality.label}</p></div></div>{quality.issues.length ? <div className="mt-3 space-y-1 text-xs leading-5 opacity-90">{quality.issues.slice(0, 4).map((issue) => <p key={issue.message}>• {issue.message}</p>)}{quality.issues.length > 4 && <p className="font-black">+ {quality.issues.length - 4} ajustes</p>}</div> : <p className="mt-3 text-xs font-black opacity-80">Sem alertas graves. Curso com boa estrutura.</p>}<div className="mt-4 flex gap-2"><button onClick={() => preencherCurso(course.id, course.title)} className="rounded-xl bg-white px-4 py-2 text-xs font-black text-black">Usar ID</button><button onClick={() => copiar(`${course.title}\nNota: ${quality.score}%\nStatus: ${quality.label}\nAjustes:\n${quality.issues.map((i) => `- ${i.message}`).join("\n") || "Sem alertas"}`)} className="rounded-xl border border-white/15 px-4 py-2 text-xs font-black">Copiar análise</button></div></div>)}</div>}</section>
  <section className="rounded-[2rem] border border-emerald-400/20 bg-emerald-500/10 p-5"><h3 className="text-2xl font-black text-emerald-100">Liberar curso manualmente</h3><p className="mt-1 text-sm text-emerald-100/75">Use quando quiser liberar por ID, sem depender de uma compra registrada.</p><div className="mt-5 grid gap-3 lg:grid-cols-[1.2fr_0.8fr_1fr_auto]"><input value={manualUserId} onChange={(e) => setManualUserId(e.target.value)} placeholder="ID do usuário" className="min-h-12 rounded-2xl border border-emerald-300/20 bg-black/35 px-4 text-sm font-bold text-white outline-none" /><input value={manualCourseId} onChange={(e) => setManualCourseId(e.target.value)} placeholder="ID do curso" className="min-h-12 rounded-2xl border border-emerald-300/20 bg-black/35 px-4 text-sm font-bold text-white outline-none" /><input value={manualCourseTitle} onChange={(e) => setManualCourseTitle(e.target.value)} placeholder="Nome do curso opcional" className="min-h-12 rounded-2xl border border-emerald-300/20 bg-black/35 px-4 text-sm font-bold text-white outline-none" /><button onClick={liberarCursoManual} disabled={manualLoading} className="rounded-2xl bg-white px-5 py-3 font-black text-black disabled:opacity-60">{manualLoading ? "Liberando..." : "Liberar curso"}</button></div></section>
  <section className="rounded-[2rem] border border-white/10 bg-white/[0.035] p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="text-2xl font-black">IDs dos cursos</h3><p className="mt-1 text-sm text-zinc-500">Use essa lista para preencher a liberação manual. Toque em "Usar" para preencher o ID e o nome.</p></div><button onClick={() => setShowCourseIds((value) => !value)} className="rounded-2xl border border-white/10 px-4 py-2 text-sm font-black text-zinc-300">{showCourseIds ? "Ocultar lista" : "Mostrar lista"}</button></div>{showCourseIds && <div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{professionalCourses.map((course) => <div key={course.id} className="rounded-2xl border border-white/10 bg-black/40 p-4"><p className="text-xs font-black uppercase tracking-[0.16em] text-zinc-500">Nome:</p><p className="mt-1 text-sm font-black text-white">{course.title}</p><p className="mt-3 text-xs font-black uppercase tracking-[0.16em] text-zinc-500">Id:</p><button onClick={() => copiar(course.id)} className="mt-1 break-all text-left text-sm font-black text-emerald-300 underline">{course.id}</button><div className="mt-4 flex gap-2"><button onClick={() => preencherCurso(course.id, course.title)} className="rounded-xl bg-white px-4 py-2 text-xs font-black text-black">Usar</button><button onClick={() => copiar(`Nome: ${course.title}\nId: ${course.id}`)} className="rounded-xl border border-white/10 px-4 py-2 text-xs font-black text-zinc-300">Copiar</button></div></div>)}</div>}</section>
  <section className="grid gap-3 rounded-[2rem] border border-white/10 bg-white/[0.025] p-3 lg:grid-cols-[1fr_auto]"><input value={busca} onChange={(event) => setBusca(event.target.value)} placeholder="Buscar por curso, ID do usuário, compra ou status..." className="min-h-12 rounded-2xl border border-white/10 bg-black/45 px-4 text-sm font-bold text-white outline-none" /><div className="flex gap-2 overflow-x-auto">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`whitespace-nowrap rounded-2xl px-4 py-2.5 text-sm font-black ${tab === item ? "bg-white text-black" : "text-zinc-400 hover:bg-white/[0.05]"}`}>{item}</button>)}</div></section>
  {erro && <div className="rounded-3xl border border-red-400/20 bg-red-500/10 p-5 text-red-100">Erro: {erro}</div>}{loading && <div className="rounded-3xl border border-white/10 bg-white/[0.035] p-6 text-zinc-400">Carregando...</div>}
  <section className="grid gap-4">{!loading && filtrados.length === 0 ? <div className="rounded-3xl border border-white/10 bg-white/[0.035] p-8 text-center text-zinc-500">Nada encontrado.</div> : filtrados.map((item) => <div key={item.id} className="rounded-[2rem] border border-white/10 bg-white/[0.035] p-5 shadow-xl shadow-black/25"><div className="grid gap-4 lg:grid-cols-[1fr_230px]"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-zinc-500">{typeLabel(item.type)} • {formatDate(item.created_at)} BRT</p><h3 className="mt-1 text-xl font-black">{item.title}</h3><button onClick={() => copiar(item.user_id)} className="mt-2 block text-left text-xs text-blue-300 underline">ID usuário: {item.user_id}</button><button onClick={() => copiar(item.id)} className="mt-1 block text-left text-xs text-zinc-500 underline">ID compra/ticket: {item.id}</button>{item.course_title && <p className="mt-3 text-sm text-zinc-400">Curso: <span className="text-zinc-200">{item.course_title}</span></p>}{item.course_id && <button onClick={() => copiar(String(item.course_id))} className="mt-1 text-left text-xs text-emerald-300 underline">ID curso: {item.course_id}</button>}{(item.total_price || item.price) && <p className="mt-2 text-sm text-zinc-400">Valor: <span className="text-white">{item.total_price || item.price}</span></p>}<p className={`mt-3 inline-flex rounded-full border px-3 py-1 text-xs font-black ${statusStyle(item.status)}`}>{item.status}</p>{item.comprovante_url && <a href={item.comprovante_url} target="_blank" rel="noreferrer" className="ml-2 text-sm font-black text-amber-100 underline">Abrir comprovante</a>}</div><div className="grid gap-2">{item.type === "purchase" && <button disabled={updatingId === item.id || !canRelease(item)} onClick={() => liberarCursoDaCompra(item)} className="rounded-xl bg-white px-4 py-3 text-sm font-black text-black disabled:opacity-50">{updatingId === item.id ? "Liberando..." : canRelease(item) ? "Liberar curso" : aprovados.includes(normalize(item.status)) ? "Já liberado" : "Sem ID do curso"}</button>}{item.type === "purchase" && <button disabled={updatingId === item.id || aprovados.includes(normalize(item.status))} onClick={() => atualizarStatus(item.id, "compra recusada")} className="rounded-xl border border-red-400/20 bg-red-500/10 px-4 py-2 text-sm font-black text-red-200 disabled:opacity-50">Recusar</button>}{item.type === "purchase" && <button disabled={updatingId === item.id || normalize(item.status) === "compra finalizada"} onClick={() => atualizarStatus(item.id, "compra finalizada")} className="rounded-xl border border-emerald-400/20 bg-emerald-500/10 px-4 py-2 text-sm font-black text-emerald-200 disabled:opacity-50">Fechar compra</button>}{item.type === "ticket" && <button disabled={updatingId === item.id || normalize(item.status) === "fechado"} onClick={() => atualizarStatus(item.id, "fechado")} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-black text-zinc-200 disabled:opacity-50">{normalize(item.status) === "fechado" ? "Ticket fechado" : "Fechar ticket"}</button>}<button onClick={() => copiar(`${typeLabel(item.type)}: ${item.title}\nStatus: ${item.status}\nID: ${item.id}\nUsuário: ${item.user_id}\nCurso: ${item.course_title ?? "-"}\nID curso: ${item.course_id ?? "-"}`)} className="rounded-xl border border-white/10 bg-black/35 px-4 py-2 text-sm font-black text-zinc-300">Copiar resumo</button></div></div></div>)}</section></div>;
}
